import React from "react";
import { Switch } from "react-router";
import App from "./App";

export default function Routes() {
    return (
        <App>
            <Switch>
                
            </Switch>
        </App>
    );
}
